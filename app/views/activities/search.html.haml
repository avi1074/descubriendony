.categories
  .show
    .container
      %h1 Search results for "#{params[:searchterm]}"
      - @activities.each_with_index do |activity, index|
        %a.activity-item-container{href: "/activities/#{activity.principal_category.to_s}/#{activity.permalink}"}
          .avatar-container
            .avatar{style: "background-image: url(#{activity.activity_image_url(:medium).to_s})"}
          .details-container
            .title
              = activity.main_title
            .description
              = truncate_html(activity.description, :ommision => "... Read More", :length => 400)
              %br
              %br
              .btn.btn-primary{href: "/activities/#{activity.principal_category.to_s}/#{activity.permalink}"}= t("activities.index.learn-more")